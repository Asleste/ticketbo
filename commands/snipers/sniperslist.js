const commando = require('discord.js-commando');

   class SniperlistCommand extends commando.Commando
   {

     constructor(client)
     {

       super(client,{
         name: 'snipers',
         group: 'snipers',
         memberName: 'SnipersList',
         description: 'Show the list of snipers'

		   });

	 }

     async run(message, args)
         {
            var change =
		 }


   }
